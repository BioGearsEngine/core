#include <biogears/filesystem/path.h>
#include <biogears/io/directories/ecg.h>

#include <biogears/io/data/config/DynamicStabilization.xml.hxx>
#include <biogears/io/data/config/TimedStabilization.xml.hxx>
//...

namespace biogears {
namespace io {
  bool generate_ecg_directory(const char* file)
  {
  }
  //-----------------------------------------------------------------------------
  bool does_embeded_ecg_file_exists(const char* file)
  {
  }
  //-----------------------------------------------------------------------------
  char const* find_ecg_file(const char* file)
  {
  }
  //-----------------------------------------------------------------------------
  char const* get_ecg_file_hash(const char* file)
  {
  }
  //-----------------------------------------------------------------------------
  char const* const* list_ecg_files()
  {
  }
  //-----------------------------------------------------------------------------
  size_t ecg_file_count()
  {
  }
  //-----------------------------------------------------------------------------
  char const* get_embeded_ecg_file(const char* file)
  {
  }
  //-----------------------------------------------------------------------------

  //biogears::filesystem::path overloads
  bool generate_ecg_directory(biogears::filesystem::path file)
  {
  }
  //-----------------------------------------------------------------------------
  bool does_embeded_ecg_file_exists(biogears::filesystem::path file)
  {
  }
  //-----------------------------------------------------------------------------
  biogears::filesystem::path find_ecg_file(biogears::filesystem::path file)
  {
  }
  //-----------------------------------------------------------------------------
  char const* get_ecg_file_hash(biogears::filesystem::path file)
  {
  }
  //-----------------------------------------------------------------------------
  char const* get_embeded_ecg_file(biogears::filesystem::path file)
  {
  }
  //-----------------------------------------------------------------------------
}
}
