#include <biogears/io/directories/environments.h>

#include <biogears/filesystem/path.h>

namespace biogears {
namespace io {
  bool generate_environments_directory(const char* file)
  {
      return false;
  }
  //-----------------------------------------------------------------------------
  bool does_embedded_environments_file_exist(const char* file)
  {
      return false;
  }
  //-----------------------------------------------------------------------------
  char const* find_environments_file(const char* file)
  {
     return nullptr;
  }
  //-----------------------------------------------------------------------------
  char const* get_environments_file_hash(const char* file)
  {
     return nullptr;
  }
  //-----------------------------------------------------------------------------
  char const* const* list_environments_files()
  {
     return nullptr;
  }
  //-----------------------------------------------------------------------------
  size_t environments_file_count()
  {
      return 0;
  }
  //-----------------------------------------------------------------------------
  char const* get_embedded_environments_file(const char* file)
  {
     return nullptr;
  }
  //-----------------------------------------------------------------------------

  //biogears::filesystem::path overloads
  bool generate_environments_directory(biogears::filesystem::path file)
  {
      return false;
  }
  //-----------------------------------------------------------------------------
  bool does_embedded_environments_file_exist(biogears::filesystem::path file)
  {
      return false;
  }
  //-----------------------------------------------------------------------------
  char const* get_environments_file_hash(biogears::filesystem::path file)
  {
     return nullptr;
  }
  //-----------------------------------------------------------------------------
  char const* get_embedded_environments_file(biogears::filesystem::path file)
  {
     return nullptr;
  }
  //-----------------------------------------------------------------------------
}
}
